version: "2"

# Linting only. Formatting stays with the pinned gofumpt step in CI
# (see .github/workflows/ci.yml).
linters:
  default: standard # errcheck, govet, ineffassign, staticcheck, unused
  enable:
    - errorlint    # correct error wrapping / comparison (funq has grooveError + Unwrap)
    - misspell     # typos in comments and strings
    - nolintlint   # keep //nolint directives honest and specific
    - predeclared  # shadowing predeclared identifiers
    - unconvert    # redundant type conversions
  exclusions:
    rules:
      # compose_test.go spells out the function type on a var on purpose:
      # that annotation is the method-value interop assertion under test.
      - path: compose_test\.go
        linters: [staticcheck]
        text: 'ST1023|QF1011'

formatters:
  enable: []

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
